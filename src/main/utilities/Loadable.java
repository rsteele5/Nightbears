package main.utilities;

/**
 * Contains the necessary functions to load information from a file.
 */
public interface Loadable {
    /**
     * Defines how a Loadable will load its needed information.
     */
    void load();
}
